# Phase S8 — Blog / novedades (6 launch posts). Sonnet. Lane 2.
Include `prompts/_lane2-common.md`. Sections: `plan.md` §3.5, §6.5.
Owns: `blog/**`, `content/blog.php`, `docs/log/s8.md`.
Rules: news voice, dated, each post links one evergreen article (may be `planned:`) and ends
with `cta-primary`. The launch post is the one page that may say "ya se puede instalar" with
the real install steps linked, not repeated. Dengue post cites MSPBS season data or stays
generic with a source. `/blog/` index = editorial big-type list, newest first.
Exit: 6 posts 500–900 words + index; `Article` schema with `datePublished`; verify green;
PR merged; log.
