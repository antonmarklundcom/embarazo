# Phase S6 — Clusters salud + alimentacion (15 articles, 2 hubs). Sonnet. Lane 2.
Include `prompts/_lane2-common.md`. Sections: `plan.md` §3.4, §6.4; pilot P-5 (tereré) in
the app repo plan §9. Read the app guides #1, #2, #6, #7 from `lib/seed/articles.json` in a
shallow clone of the app repo — expand, never contradict.
Owns: `salud/**`, `alimentacion/**`, their rows in `content/articulos.php`, the
`salud`/`alimentacion` records of `content/clusters.php`, `docs/log/s6.md`.
Rules: kind medical → disclaimer + ≥ 1 primary source per page (MSPBS, PAI, OPS/OMS). Alarm
signs article: 141/911 `tel:` links, Guaraní strip, "guardá tus contactos en la app" hand-off.
Vaccines: one H2 per PAI vaccine with the window; the yuyos table is row-by-row sourced. No
"debés"; "muchas obstetras recomiendan…" + citation. Fan out after two exemplars.
Exit: 15 articles, 2 hubs; verify green; PR merged; log.
