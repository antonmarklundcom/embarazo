# Phase S5 — Clusters derechos + tramites (16 articles, 2 hubs). Sonnet. Lane 2.
Include `prompts/_lane2-common.md`. Sections: `plan.md` §3.4, §6.4; bar = T1's
`licencia-de-maternidad` exemplar; pilot P-4 (carné perinatal) in the app repo plan §9.
Owns: `derechos/**` (except the two T1 files, which you may complete), `tramites/**`, their
rows in `content/articulos.php`, the `derechos`/`tramites` records of `content/clusters.php`,
`docs/log/s5.md`.
Rules: every legal figure cites the law article in `sources[]` and the page carries
`validAsOf`; numbers from `docs/app-facts.md` only, else "consultá el monto vigente" + a line
in `docs/facts-to-verify.md`. "Si tu empleador no cumple" names MTESS. Rights hub shows the
Guaraní strip. Procedural pages carry `steps[]` and a documents table. Fan out after two
exemplars (one per cluster).
Exit: 16 articles 800–1,500 words, 2 hubs; verify green; PR merged; log.
