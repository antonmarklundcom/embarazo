# Phase S4 — Tools: /calculadora and /calendario-de-ovulacion. Sonnet. Lane 2.
Include `prompts/_lane2-common.md`. Sections: `plan.md` §3.3, §6.3. Read
`design/preview/calculadora.html` and `docs/app-facts.md` (pregnancy math).
Owns: `calculadora/**`, `calendario-de-ovulacion/**`, `content/tools.php`,
`assets/js/tools/**`, `tests/pregnancy.test.mjs`, `docs/log/s4.md`.
Rules: port `lib/pregnancy.ts` from a shallow clone of the app repo to
`assets/js/tools/pregnancy.js` (ES module, no deps) and its test vectors to
`tests/pregnancy.test.mjs` (node:test); both tools use `window.Market` date formatting.
Nothing is sent anywhere and the page says so. Result card reads the week's size from a small
JSON `assets/js/tools/semanas.json` generated by a script in your JS dir from
`content/semanas.php` (`php tools/export-weeks.php` — add it under `assets/js/tools/`, not
`tools/`). Ovulation page: fertile window = cycle length − 14 ± 5 days, loud "no es un método
anticonceptivo" above the result, hand-off `modo=planeando`. `WebApplication` schema.
Exit: tests green; FUM 2026-03-02 on 2026-09-20 → semana 29 · 28 semanas y 6 días · FPP
2026-12-07; both pages work with JS disabled (form posts nothing, shows the explanation);
verify green; PR merged; log.
