# P1 — Product pages — 2026-09-20

## Built
- Authored seven indexable product records and eight three-line page routes; home and other records preserved.
- /app/: seven single-anchor feature tiles, distinct root-link campaigns, both modes and week-20 phone fallback.
- /instalar/: Android first/open, iPhone second, #android/#iphone details anchors, general steps and two HowTo blocks.
- /preguntas-frecuentes/: 12 questions; /familia/: practical copy for partner and grandmother.
- /privacidad/: conditional data story plus three allow-listed legal links; /sobre/: reviewer en proceso and correction policy.
- /app/comparacion/: seven-question buying guide without competitor claims.
- /contacto/: noindex stub because every contact channel in content/site.php is null.
- Every record has dated metadata, FAQ, named public sources with null URL/access date, null reviewer and existing related paths.
- Consequential content/cta.php edit removes the unsupported offline claim from shared trust copy.
- Product sources render after FAQ and before closing CTA; app_page_link restricts destinations to three legal routes.
- Updated the delegated CTA href rule; retained only the related-path audit warning in the link-pass request.

## Decisions
- Manager authorized the limited page-template blocks and new helper; no network, downloads, git writes or app-repo changes.
- Kept the five established bento items in mobile order, then added planning and guides.
- No unverified browser menu path, app size, tap count, update or offline-completeness claim.
- Source URLs remain null; the pre-existing home source URL is outside p1 record ownership.
- Preserved real related page paths; the existing audit treats page related entries as article slugs and warns.
- Corrected PowerShell pipe encoding loss and explicitly checked UTF-8/accented text before final gates.
- Packaging uses SKIP_CSS_MINIFY=1 to preserve both existing stylesheet files.
- Feature-specific app destinations, contact-stub trust strips and screenshots explicitly skipped per manager; existing CTA rule lives in tests/jsonld.mjs, invoked by verify.sh, so only that rule changed there.

## Known issues
- Existing related-path audit warnings remain; product contextual internal links work, but no related-card block is added.
- No visual/Lighthouse pass claimed; screenshots are explicitly excluded from the manager's revised acceptance.
- Whole-file grep finds the unchanged home bibliography app URL; new p1 records/routes contain none.
- Named comparison needs a network-enabled human; contact channel, reviewer and device/offline validation remain human follow-ups.

Verification: PASS php -l (12 touched PHP files), eight-record metadata/UTF-8/source/link checks, app_page_link and bento fixtures (13 rejected helper paths), actual CTA rule fixtures (5 accepted/12 rejected URLs), curl/DOM checks (8 HTTP 200, Android-first IDs, seven single-anchor app tiles, three legal links, sources after FAQ/before CTA, exactly two trust strips on each authored product page; server stopped), bash ./verify.sh --port 8743, SKIP_CSS_MINIFY=1 bash ./deploy/make-zip.sh, bash ./verify.sh --port 8743 --root dist/embarazo-2026-09-20 and read-only git status/diff checks. Both gates: 75 routes, 51 JSON-LD blocks, WARN only for related paths/content backlog. Both stylesheet hashes unchanged. Revised manager acceptance PASS; screenshots/contact-stub strips/feature-specific destinations excluded as instructed.
