# T1 — Foundation

## STAGE A

### Built
- Imported 42 app seed records with a dependency-free local Node importer; authored week 20.
- Added three trimester records, six 200–300-word hub intros, and the legal article exemplar.
- Added CTA/disclaimer copy and the empty tools handoff/math contract.
- Added share/trimester/disclaimer helpers; corrected the existing T0 app_link query order.
- Added pure JSON-LD builders; kept legacy rendering unchanged for stage B.
- Logged unresolved clinical, legal, offline and Guaraní facts without invented URLs.
- PASS: eight PHP lints, importer rerun, php -r content/helper/JSON-LD checks, repo verify, make-zip and staged verify (32 routes).
- Manager revision: rewrote clinical week-20 and legal exemplar copy; replaced internal-file citations throughout public source arrays.
- Revision checks PASS: four touched PHP lints, php -r (42 weeks/metadata/sources; week body 898 words, five FAQs; article total 1178, lead 64), repo verify, make-zip and staged verify (32 routes).

### Decisions
- Week pages answer within 100 words: concrete development, common symptoms, controls and questions in es-PY voseo; use conservative clinical ranges, no promises/doses/diagnoses or advice to delay care. Alarm copy points to /salud/senales-de-alarma/ and its related slug; no meta-disclaimer filler.
- Legal/admin/product facts stay within verified scope with one brief qualification per unresolved condition; public sources name real publications/publishers, never repo files. Clinical pages are logged for medical review; disclaimer partial carries general caution.
- Plain-text copy uses e() at HTML boundary; URL helpers RFC3986-encode, json_ld() alone encodes scripts. app_link keeps defaults, accepts w/fpp/fum/modo only, invalid week/date inputs throw.
- Reimport preserves editorial fields, refreshes size/milestone/updated; seed-only rows stay noindex. Trimester weeks derive via week_trimester() after bootstrap; timelines are editorial reminders.
- Cluster/trimester additions: sources, reviewedBy, updated; hubs also nullable validAsOf. Article sections retain items/nullable table, steps {title,body[]}, related article slugs; hub derives from cluster.
- CTA variants are strings; cta.disclaimers feeds disclaimer_kind(); reviewedBy is {name,credentials,registration}|null.
- Stage B uses jsonld_sitewide/editorial plus explicit URL builders; replace legacy Organization, avoid duplicate blocks; FAQ builder reused.

### Known issues
- Public bibliography names OMS/MSPBS guidance, laws, IPS guidance and public app guides; no fresh consultation implied, URLs/accessed null, reviewer null.
- Planned related slugs need routes/content in S5–S7; Guaraní strings need review before rendering.
- Exact required trust copy retained; offline scope still pending verification (docs/facts-to-verify.md).
- php-site-template skill unavailable in installed skill folders; followed the repository's existing conventions.
- Stage B/C own template wiring, routes, language markup, expanded gates, home and visual validation.


## STAGE B

### Built
- Translated the D0 component classes into production CSS; removed legacy/form/gallery and absent-selector rules; generated site.min.css.
- Added contextual CTAs, trust, kind-aware disclaimers, sources, share, week navigation/grid, phone mock and review-gated Guaraní.
- Rebuilt header/footer/breadcrumbs/FAQ with native details; added checkbox-dismissed content-only mobile CTA and height spacer.
- Added week, cluster/week hub and trimester templates; extended editorial/blog and product page rendering with tables, steps, bento and HowTo.
- Wired Stage A JSON-LD in head.php: sitewide identity/trails, editorial types/FAQ, product application, tool application and per-OS HowTo.
- Activated three-line week-20, derechos hub and maternity-leave routes; sitemap now includes authored week/article routes.
- PASS: requested route curl assertions, PHP lints, minifier, repo/staged verify, deploy zip, URL/CSS grep, product/trimester/reviewer fixture rendering.

### Decisions
- Routes bootstrap through templates: week($n:1..42), trimester($n:1..3), hub($cluster; 'semana' reads pages['/semana/']), article($slug), page($path); missing records return 404, seed weeks stay noindex.
- article reads articulos first, then legacy blog($sections/$faq); sections support body/items/table, steps title/body, appHandoff text/medium/extra; missing related records are omitted until authored, hub link retained.
- page accepts features key/title/text/appPath/tone/extra, howto os/steps (string or title/body), faq, optional phoneWeek/phoneImage; Android opens by OS label. appPath is not an app_link parameter: campaigns identify tiles; only supported extra parameters pass through.
- head reads page.record/kind, faq, tool, howto, jsonld; builds sitewide and breadcrumbs once; json_ld is the sole encoder. Keep site.css href: packaging replaces it with minified CSS and removes site.min.css.
- CTA partials consume optional ctaOptions {title,text,medium,campaign,extra}; cta-week also requires $n. cta-band wraps primary; trust-strip needs no locals. All app destinations use app_link.
- disclaimerRecord {kind,reviewedBy,validAsOf}, sourcesRecord {sources,updated}, faqItems/faqTitle, bodySections, relatedSlugs/relatedHub and gridWeeks are consumed; partials clean prefixed scratch locals. breadcrumbs/wa-share use page; week-nav uses $n.
- phone-frame consumes phoneWeek record/phoneNumber and optional phoneImage {src,alt}; size and progress derive from that record/number. guarani-strip consumes guaraniItems {es,gn} and guaraniReviewed=true; only rights hub/emergency article may invoke it.
- Content templates set page.sticky; footer includes sticky-cta once after itself, using page.path/weekNumber. Product pages have hero/closing CTA and no sticky. JS only enhances the plain WhatsApp share anchor.

### Known issues
- BLOCKED: pre-existing opening CSS comment consumes ':root {'; protected token bytes left unchanged, repair authorization requested. Brand rendering cannot be approved until repaired.
- Browser permission denied localhost access; no visual screenshot/360px acceptance claimed. PHP fixture checks do not replace browser QA.
- Related records and five-child hubs await S5–S7; Guaraní remains hidden pending native review; appPath cannot bypass Stage A's supported onboarding parameters.
- Consequential scope edits: content/pages.php clears derechos stub/noindex; sitemap.php includes authored, existing week/article routes. Other stubs remain stubs; Stage C still owns expanded verification/home.
- php-site-template skill remains unavailable in installed skill/plugin folders; followed local contracts. No network, app-repo changes, git writes or protected-file edits.

## STAGE C

### Built
- Replaced the home stub with the preview's split hero, record-driven phone, three proofs, week teaser, bento, FAQ and closing CTA; all home copy lives in pages['/'], all app links use app_link, no sticky bar.
- Home product copy traces to docs/app-facts.md; its trust override omits unverified offline/installation claims. Existing shared trust copy remains a previously logged issue; no new unverified claims were introduced.
- Added rich(): escape the whole string first, then allow only [label](/internal/path/#anchor). Leads, body/items/steps, FAQ answers, handoff text and hub intros use it; titles and table cells remain escaped plain text. Week-20 alarm prose now links; the legal exemplar had no bare prose paths to convert.
- Added fmt_measure() for decimal commas, trimmed zeroes and missing measurements; week panels and phone use it. Initial Stage C removed five unused legacy responsive tokens; this review preserves the manager's subsequently repaired stylesheet.
- Added the 41 missing three-line week routes; seed weeks remain 200/noindex and excluded from sitemap. The route contract now derives 75 URLs from all seven content collections plus static endpoints/security probes; there is no planned-slug list to maintain.
- deploy/routes.php supplies status/indexability, --audit-data and --check-files from one inventory. Missing-file errors identify the absolute file and exact three-line route to create. Existing page stub/noindex flags remain defaults for overlapping collection records; an explicit collection flag takes precedence.
- Normal source checks apply only to indexable medical/legal records and authored weeks; empty seed weeks are skipped. --final requires all three sections and at least two sources for every week, and promotes content-volume warnings to failures.
- tests/css-sanity.mjs checks comment boundaries, brace balance and exactly two measured :root blocks (main tokens and 64rem desktop override). Both source and packaged CSS run through this always-fail gate; seven corruption fixtures were rejected.
- Small consequential edits keep collection-template noindex flags and article/blog/week sitemap exclusions aligned with the route contract. deploy/make-zip.sh accepts SKIP_CSS_MINIFY=1 so this review packages the manager-approved CSS without regenerating either stylesheet.
- Consequential fixes: lib/seo.php supplies required application fields and medical headline, and omits the brand suffix when it would exceed 60 characters. fmt_measure() extends helpers beyond the rich-only file note to satisfy item 9. Product bento text is outside its CTA anchor so rich links cannot nest anchors.
- Watcher phase list now specifies S1–S8, Sonnet, at most four concurrent, then L; S9 imagery stays manual. No sessions or Routine were created.
- Manager review round 3: home bento tiles are plain anchors with content-driven decorative icons, headings, unchanged copy/targets and link-arrow spans; no tile trust strips or nested CTA buttons. Home retains exactly two trust strips (hero/close), enforced directly in verify.sh in normal, --final and --root runs. No shared tile partial is used; the separate product-page tile variant already has no strip. No CSS changes or regeneration.

### Verification and remaining constraints
- Manager review corrects the original unconditional source requirement: seed records do not block normal verification. No seed content was changed to manufacture sources.
- PASS: seven PHP lints for this review; CSS source/minified sanity and seven corruption cases; nine source-gate cases; a synthetic content root proves all seven collections, indexability and 11 exact three-line missing-file fixes. Prior rich(), decimal, prose-path and HTTP assertions remain recorded in the initial Stage C work.
- PASS: bash ./verify.sh; bash ./deploy/make-zip.sh with SKIP_CSS_MINIFY=1 (128 files, both stylesheet hashes unchanged during packaging); bash ./verify.sh --root dist/embarazo-2026-09-20. Both gates check 75 routes, 65 metadata records, HTML/CTA/size rules, CSS sanity and 20 JSON-LD blocks on four indexable pages.
- FAIL (expected): bash ./verify.sh --final exits 1 with 41 week-readiness records, 5 dangling related slugs, 1 article with fewer than two related articles, 41 weeks without an article link, and 10 hubs with fewer than five children (98 total).
- Port 8097/live browser checks belong to the manager; verify.sh's own server is the gate. No browser screenshots or Lighthouse score claimed. Neither CSS file was edited or regenerated in this review; external stylesheet changes observed during the session were preserved.

## FINAL
- Routes are derived from content. Lane-2 workers only add/edit records plus three-line route files; never edit deploy/routes.php. Missing-file checks print the exact three lines to create.
- Week: fill semanas[n], including all three sections, sources and FAQ; route sets $n and requires templates/week.php. Empty sections stay noindex and out of the sitemap.
- Article/blog: add the documented record; route sets $slug and requires templates/article.php. An article's path must match /<cluster>/<slug>/; stub records remain noindex.
- Hub child: add an article with the matching cluster; hub cards derive automatically. Hub route sets $cluster ('semana' selects the week hub); clear existing page stub/noindex flags when authored.
- Other locals: page.php expects $path; trimester.php expects $n (1–3); tool.php expects $slug and escaped $toolCalcHtml. The tool scaffold initializes the latter for S4 to fill.
- Write concrete week education, answer within 100 words, use conservative ranges and no meta-disclaimers; name sources and log pending medical review. No doses, diagnoses or delaying care.
- Inline prose links use [label](/path/); rich() escapes first. Titles/labels/table cells stay plain; fmt_measure() formats record measurements at render time.
- Related slugs must exist or start planned:; planned entries do not count as rendered links. All app destinations use app_link().
- Normal verify requires sources only for indexable medical/legal records and authored weeks; unfilled seed weeks are skipped. --final requires every week to have all three sections and at least two sources, and makes content-volume warnings fail.
- Indexable now: /, /semana/20/, /derechos/, /derechos/licencia-de-maternidad/. Other existing pages are stubs; future URLs enter the checks when their content records are added.
